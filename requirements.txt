# ZynthoBrain — requirements
# Install with: pip install -r requirements.txt

# Core ML
torch>=2.1.0
torchvision>=0.16.0

# HuggingFace stack
transformers>=4.41.0
datasets>=2.18.0
accelerate>=0.27.0
peft>=0.10.0
trl>=0.9.0

# 4-bit QLoRA quantization (requires CUDA)
bitsandbytes>=0.43.0

# Logging / monitoring
tensorboard>=2.14.0

# Optional: swap tensorboard for W&B
# wandb>=0.16.0
